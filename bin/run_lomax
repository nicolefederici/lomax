#!/usr/bin/env ruby

require_relative "../lib/lomax.rb"

Lomax::CommandLineInterface.new.run
# new_instance_of_cli = Lomax::CommandLineInterface.new
# new_instance_of_cli.run

#this is called the "executable file" it gets the whole thing running, which is why you type bin/run_lomax at the start og the gem in the command line.
